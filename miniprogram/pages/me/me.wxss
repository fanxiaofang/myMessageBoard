.avatar-wrapper {
  width: 100px; /* 设置合适的宽度 */
  height: 100px; /* 设置合适的高度 */
  overflow: hidden; /* 确保图片不会超出容器 */
  padding: 0;
  margin: 0;
  border: none;
  background: none;
}

.avatar {
  width: 100%; /* 图片宽度填满容器 */
  height: 100%; /* 图片高度填满容器 */
  object-fit: cover; /* 保持宽高比的同时填充整个容器 */
}